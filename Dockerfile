# Используем легкий образ Nginx
FROM nginx:alpine

# Копируем все файлы проекта в папку, которую обслуживает Nginx
COPY . /usr/share/nginx/html/

# Открываем стандартный порт для веб-трафика
EXPOSE 80

# Запускаем Nginx (он сам стартует при запуске контейнера)
CMD ["nginx", "-g", "daemon off;"]
